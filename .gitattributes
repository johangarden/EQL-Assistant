# Normalize line endings to LF in the repo and on checkout (silences the
# "LF will be replaced by CRLF" warnings on Windows).
* text=auto eol=lf

# Treat binary assets as binary.
*.ico binary
*.png binary
