<Application x:Class="EQLOverlay.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EQLOverlay"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Palette + shared button styles + app-wide tooltips/menus.
                     Dialog-only control styles live in Themes/Controls.xaml,
                     merged per dialog window. -->
                <ResourceDictionary Source="/Themes/Palette.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <local:FillStarConverter x:Key="FillStar"/>
            <local:RestStarConverter x:Key="RestStar"/>
            <local:BottomSpacingConverter x:Key="BottomSpacing"/>
            <local:MultiplyConverter x:Key="Multiply"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
