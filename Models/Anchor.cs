namespace EQLOverlay.Models;

/// <summary>Which screen corner a panel is pinned to (and grows away from).</summary>
public enum Anchor
{
    TopLeft,
    TopRight,
    BottomLeft,
    BottomRight,
}
