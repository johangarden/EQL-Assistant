<UserControl x:Class="EQLOverlay.Views.FightSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             UseLayoutRounding="True"
             TextOptions.TextFormattingMode="Display">

    <!-- StaticResource resolves at parse time — merge the theme locally. -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- The fight fact sheet: actor tabs over one sortable column table. -->
    <StackPanel>
        <TextBlock x:Name="TitleText" Foreground="#C9D4E3" FontWeight="Bold" FontSize="13"
                   Margin="0,0,0,4" Visibility="Collapsed"/>
        <WrapPanel x:Name="TabsPanel" Margin="0,0,0,4"/>
        <WrapPanel x:Name="AspectsPanel" Margin="0,0,0,6"/>
        <Border Background="#1B2130" BorderBrush="#3A4560" BorderThickness="1"
                CornerRadius="8" Padding="12,4,12,8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <Grid x:Name="TableHost"/>
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>
